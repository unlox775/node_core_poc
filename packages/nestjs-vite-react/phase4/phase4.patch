diff --git b/packages/nestjs-vite-react/api/src/app.module.ts a/packages/nestjs-vite-react/api/src/app.module.ts
index 4bedd061..c4f4be23 100644
--- b/packages/nestjs-vite-react/api/src/app.module.ts
+++ a/packages/nestjs-vite-react/api/src/app.module.ts
@@ -1,9 +1,10 @@
 import { Module } from '@nestjs/common';
 import { DealsModule } from './deals/deals.module';
 import { AdminModule } from './admin/admin.module';
+import { ContactsModule } from './contacts/contacts.module';
 import { PrismaModule } from './prisma/prisma.module';
 
 @Module({
-  imports: [PrismaModule, DealsModule, AdminModule],
+  imports: [PrismaModule, DealsModule, AdminModule, ContactsModule],
 })
 export class AppModule {}
diff --git b/packages/nestjs-vite-react/api/src/contacts/contacts.controller.ts a/packages/nestjs-vite-react/api/src/contacts/contacts.controller.ts
new file mode 100644
index 00000000..9108b500
--- /dev/null
+++ a/packages/nestjs-vite-react/api/src/contacts/contacts.controller.ts
@@ -0,0 +1,17 @@
+import { Controller, Get, Post, Body } from '@nestjs/common';
+import { ContactsService } from './contacts.service';
+
+@Controller('api/contacts')
+export class ContactsController {
+  constructor(private readonly contacts: ContactsService) {}
+
+  @Get()
+  list() {
+    return this.contacts.findAll();
+  }
+
+  @Post()
+  create(@Body() body: { name: string; email: string; phone: string }) {
+    return this.contacts.create(body);
+  }
+}
diff --git b/packages/nestjs-vite-react/api/src/contacts/contacts.module.ts a/packages/nestjs-vite-react/api/src/contacts/contacts.module.ts
new file mode 100644
index 00000000..a2e4b548
--- /dev/null
+++ a/packages/nestjs-vite-react/api/src/contacts/contacts.module.ts
@@ -0,0 +1,11 @@
+import { Module } from '@nestjs/common';
+import { ContactsController } from './contacts.controller';
+import { ContactsService } from './contacts.service';
+import { PrismaModule } from '../prisma/prisma.module';
+
+@Module({
+  imports: [PrismaModule],
+  controllers: [ContactsController],
+  providers: [ContactsService],
+})
+export class ContactsModule {}
diff --git b/packages/nestjs-vite-react/api/src/contacts/contacts.service.ts a/packages/nestjs-vite-react/api/src/contacts/contacts.service.ts
new file mode 100644
index 00000000..773c598e
--- /dev/null
+++ a/packages/nestjs-vite-react/api/src/contacts/contacts.service.ts
@@ -0,0 +1,15 @@
+import { Injectable } from '@nestjs/common';
+import { PrismaService } from '../prisma/prisma.service';
+
+@Injectable()
+export class ContactsService {
+  constructor(private prisma: PrismaService) {}
+
+  findAll() {
+    return this.prisma.contact.findMany({ orderBy: { name: 'asc' } });
+  }
+
+  create(data: { name: string; email: string; phone: string }) {
+    return this.prisma.contact.create({ data });
+  }
+}
diff --git b/packages/nestjs-vite-react/api/src/deals/deals.controller.ts a/packages/nestjs-vite-react/api/src/deals/deals.controller.ts
index a72cc8aa..11f97ec7 100644
--- b/packages/nestjs-vite-react/api/src/deals/deals.controller.ts
+++ a/packages/nestjs-vite-react/api/src/deals/deals.controller.ts
@@ -11,7 +11,7 @@ export class DealsController {
   }
 
   @Post()
-  create(@Body() body: { name: string; description: string; address: string }) {
+  create(@Body() body: { name: string; description: string; address: string; contact?: { name: string; email: string; phone: string } }) {
     return this.deals.create(body);
   }
 
@@ -24,4 +24,14 @@ export class DealsController {
   remove(@Param('id') id: string) {
     return this.deals.remove(id);
   }
+
+  @Post(':id/contacts')
+  addContact(@Param('id') dealId: string, @Body() body: { contactId: string }) {
+    return this.deals.addContact(dealId, body.contactId);
+  }
+
+  @Delete(':id/contacts/:contactId')
+  removeContact(@Param('id') dealId: string, @Param('contactId') contactId: string) {
+    return this.deals.removeContact(dealId, contactId);
+  }
 }
diff --git b/packages/nestjs-vite-react/api/src/deals/deals.service.ts a/packages/nestjs-vite-react/api/src/deals/deals.service.ts
index f79edb19..7b2a39c8 100644
--- b/packages/nestjs-vite-react/api/src/deals/deals.service.ts
+++ a/packages/nestjs-vite-react/api/src/deals/deals.service.ts
@@ -6,11 +6,26 @@ export class DealsService {
   constructor(private prisma: PrismaService) {}
 
   findAll() {
-    return this.prisma.deal.findMany({ orderBy: { createdAt: 'desc' } });
+    return this.prisma.deal.findMany({
+      orderBy: { createdAt: 'desc' },
+      include: { contacts: { include: { contact: true } } },
+    }).then((deals) =>
+      deals.map((d) => ({ ...d, contacts: d.contacts.map((dc) => dc.contact) }))
+    );
   }
 
-  create(data: { name: string; description: string; address: string }) {
-    return this.prisma.deal.create({ data: { ...data, status: 'pending' } });
+  create(data: { name: string; description: string; address: string; contact?: { name: string; email: string; phone: string } }) {
+    const { contact, ...dealData } = data;
+    const createData: Parameters<typeof this.prisma.deal.create>[0]['data'] = { ...dealData, status: 'pending' };
+    if (contact) {
+      createData.contacts = {
+        create: [{ contact: { create: contact } }],
+      };
+    }
+    return this.prisma.deal.create({
+      data: createData,
+      include: { contacts: { include: { contact: true } } },
+    }).then((d) => ({ ...d, contacts: d.contacts.map((dc) => dc.contact) }));
   }
 
   update(id: string, data: { name?: string; description?: string; address?: string; status?: string }) {
@@ -20,4 +35,12 @@ export class DealsService {
   remove(id: string) {
     return this.prisma.deal.delete({ where: { id } });
   }
+
+  async addContact(dealId: string, contactId: string) {
+    await this.prisma.dealContact.create({ data: { dealId, contactId } });
+  }
+
+  async removeContact(dealId: string, contactId: string) {
+    await this.prisma.dealContact.delete({ where: { dealId_contactId: { dealId, contactId } } });
+  }
 }
diff --git b/packages/nestjs-vite-react/prisma/schema.prisma a/packages/nestjs-vite-react/prisma/schema.prisma
index 6bc7cf85..8d79689c 100644
--- b/packages/nestjs-vite-react/prisma/schema.prisma
+++ a/packages/nestjs-vite-react/prisma/schema.prisma
@@ -15,6 +15,16 @@ model AdminUser {
   updatedAt DateTime @updatedAt
 }
 
+model Contact {
+  id        String   @id @default(cuid())
+  name      String
+  email     String
+  phone     String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  deals     DealContact[]
+}
+
 model Deal {
   id          String   @id @default(cuid())
   name        String
@@ -23,4 +33,13 @@ model Deal {
   status      String   @default("pending")
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
+  contacts    DealContact[]
+}
+
+model DealContact {
+  dealId    String
+  contactId String
+  deal      Deal    @relation(fields: [dealId], references: [id], onDelete: Cascade)
+  contact   Contact @relation(fields: [contactId], references: [id], onDelete: Cascade)
+  @@id([dealId, contactId])
 }
diff --git b/packages/nestjs-vite-react/src/App.tsx a/packages/nestjs-vite-react/src/App.tsx
index 639adb70..01297042 100644
--- b/packages/nestjs-vite-react/src/App.tsx
+++ a/packages/nestjs-vite-react/src/App.tsx
@@ -5,6 +5,7 @@ import ThankYou from "./pages/ThankYou";
 import AdminLogin from "./pages/AdminLogin";
 import AdminDeals from "./pages/AdminDeals";
 import AdminEditDeal from "./pages/AdminEditDeal";
+import AdminContacts from "./pages/AdminContacts";
 
 export default function App() {
   return (
@@ -15,6 +16,7 @@ export default function App() {
       <Route path="/admin" element={<AdminLogin />} />
       <Route path="/admin/deals" element={<AdminDeals />} />
       <Route path="/admin/deals/:id" element={<AdminEditDeal />} />
+      <Route path="/admin/contacts" element={<AdminContacts />} />
     </Routes>
   );
 }
diff --git b/packages/nestjs-vite-react/src/pages/AdminContacts.tsx a/packages/nestjs-vite-react/src/pages/AdminContacts.tsx
new file mode 100644
index 00000000..682be0ec
--- /dev/null
+++ a/packages/nestjs-vite-react/src/pages/AdminContacts.tsx
@@ -0,0 +1,62 @@
+import { useEffect, useState } from "react";
+import { Link, useNavigate } from "react-router-dom";
+
+type Contact = { id: string; name: string; email: string; phone: string };
+
+export default function AdminContacts() {
+  const navigate = useNavigate();
+  const [contacts, setContacts] = useState<Contact[]>([]);
+  const [loading, setLoading] = useState(true);
+  const [name, setName] = useState("");
+  const [email, setEmail] = useState("");
+  const [phone, setPhone] = useState("");
+
+  const load = () => fetch("/api/contacts").then((r) => r.json()).then(setContacts);
+
+  useEffect(() => {
+    if (sessionStorage.getItem("admin") !== "1") {
+      navigate("/admin");
+      return;
+    }
+    load().finally(() => setLoading(false));
+  }, [navigate]);
+
+  const create = (e: React.FormEvent) => {
+    e.preventDefault();
+    fetch("/api/contacts", {
+      method: "POST",
+      headers: { "Content-Type": "application/json" },
+      body: JSON.stringify({ name, email, phone }),
+    }).then(() => {
+      setName("");
+      setEmail("");
+      setPhone("");
+      load();
+    });
+  };
+
+  return (
+    <main>
+      <h1>Admin — Contacts</h1>
+      <p><Link to="/admin">← Back to admin</Link> | <Link to="/admin/deals">Deals</Link> | <Link to="/">Home</Link></p>
+      <h2>Create contact</h2>
+      <form onSubmit={create}>
+        <div><label>Name</label><input value={name} onChange={(e) => setName(e.target.value)} required /></div>
+        <div><label>Email</label><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></div>
+        <div><label>Phone</label><input value={phone} onChange={(e) => setPhone(e.target.value)} required /></div>
+        <button type="submit">Create</button>
+      </form>
+      <h2>All contacts</h2>
+      {loading ? <p>Loading...</p> : (
+        <ul style={{ listStyle: "none", padding: 0 }}>
+          {contacts.map((c) => (
+            <li key={c.id} style={{ border: "1px solid #ccc", padding: "1rem", marginBottom: "0.5rem", borderRadius: 4 }}>
+              <strong>{c.name}</strong>
+              <p><small>{c.email} · {c.phone}</small></p>
+            </li>
+          ))}
+        </ul>
+      )}
+    </main>
+  );
+}
diff --git b/packages/nestjs-vite-react/src/pages/AdminDeals.tsx a/packages/nestjs-vite-react/src/pages/AdminDeals.tsx
index 220125e1..a48b6e1d 100644
--- b/packages/nestjs-vite-react/src/pages/AdminDeals.tsx
+++ a/packages/nestjs-vite-react/src/pages/AdminDeals.tsx
@@ -1,7 +1,8 @@
 import { useEffect, useState } from "react";
 import { Link, useNavigate } from "react-router-dom";
 
-type Deal = { id: string; name: string; description: string; address: string; status: string };
+type Contact = { id: string; name: string; email: string; phone: string };
+type Deal = { id: string; name: string; description: string; address: string; status: string; contacts?: Contact[] };
 
 export default function AdminDeals() {
   const navigate = useNavigate();
@@ -25,7 +26,7 @@ export default function AdminDeals() {
   return (
     <main>
       <h1>Admin — Deals</h1>
-      <p><Link to="/admin">← Back to admin</Link> | <Link to="/">Home</Link></p>
+      <p><Link to="/admin">← Back to admin</Link> | <Link to="/admin/contacts">Contacts</Link> | <Link to="/">Home</Link></p>
       {loading ? <p>Loading...</p> : (
         <ul style={{ listStyle: "none", padding: 0 }}>
           {deals.map((d) => (
@@ -33,6 +34,9 @@ export default function AdminDeals() {
               <strong>{d.name}</strong>
               <p>{d.description}</p>
               <p><small>{d.address} · Status: {d.status}</small></p>
+              {d.contacts && d.contacts.length > 0 && (
+                <p><small>Contacts: {d.contacts.map((c) => c.name).join(", ")}</small></p>
+              )}
               <p>
                 <Link to={`/admin/deals/${d.id}`}>Edit</Link>
                 {" · "}
diff --git b/packages/nestjs-vite-react/src/pages/AdminEditDeal.tsx a/packages/nestjs-vite-react/src/pages/AdminEditDeal.tsx
index f62673fa..597faa7e 100644
--- b/packages/nestjs-vite-react/src/pages/AdminEditDeal.tsx
+++ a/packages/nestjs-vite-react/src/pages/AdminEditDeal.tsx
@@ -1,7 +1,8 @@
 import { useEffect, useState } from "react";
 import { Link, useNavigate, useParams } from "react-router-dom";
 
-type Deal = { id: string; name: string; description: string; address: string; status: string };
+type Contact = { id: string; name: string; email: string; phone: string };
+type Deal = { id: string; name: string; description: string; address: string; status: string; contacts?: Contact[] };
 
 export default function AdminEditDeal() {
   const navigate = useNavigate();
@@ -10,6 +11,8 @@ export default function AdminEditDeal() {
   const [description, setDescription] = useState("");
   const [address, setAddress] = useState("");
   const [status, setStatus] = useState("pending");
+  const [contacts, setContacts] = useState<Contact[]>([]);
+  const [allContacts, setAllContacts] = useState<Contact[]>([]);
   const [loading, setLoading] = useState(true);
 
   useEffect(() => {
@@ -17,18 +20,20 @@ export default function AdminEditDeal() {
       navigate("/admin");
       return;
     }
-    fetch("/api/deals")
-      .then((r) => r.json())
-      .then((deals: Deal[]) => {
-        const d = deals.find((x) => x.id === id);
-        if (d) {
-          setName(d.name);
-          setDescription(d.description);
-          setAddress(d.address);
-          setStatus(d.status);
-        }
-      })
-      .finally(() => setLoading(false));
+    Promise.all([
+      fetch("/api/deals").then((r) => r.json()),
+      fetch("/api/contacts").then((r) => r.json()),
+    ]).then(([deals, contactsList]: [Deal[], Contact[]]) => {
+      const d = deals.find((x) => x.id === id);
+      if (d) {
+        setName(d.name);
+        setDescription(d.description);
+        setAddress(d.address);
+        setStatus(d.status);
+        setContacts(d.contacts || []);
+      }
+      setAllContacts(contactsList);
+    }).finally(() => setLoading(false));
   }, [id, navigate]);
 
   const save = (e: React.FormEvent) => {
@@ -40,6 +45,25 @@ export default function AdminEditDeal() {
     }).then(() => navigate("/admin/deals"));
   };
 
+  const addContact = (contactId: string) => {
+    fetch(`/api/deals/${id}/contacts`, {
+      method: "POST",
+      headers: { "Content-Type": "application/json" },
+      body: JSON.stringify({ contactId }),
+    }).then(() => {
+      const c = allContacts.find((x) => x.id === contactId);
+      if (c) setContacts((prev) => [...prev, c]);
+    });
+  };
+
+  const removeContact = (contactId: string) => {
+    fetch(`/api/deals/${id}/contacts/${contactId}`, { method: "DELETE" }).then(() =>
+      setContacts((prev) => prev.filter((c) => c.id !== contactId))
+    );
+  };
+
+  const availableToAdd = allContacts.filter((c) => !contacts.some((x) => x.id === c.id));
+
   if (loading) return <p>Loading...</p>;
 
   return (
@@ -60,6 +84,25 @@ export default function AdminEditDeal() {
         </div>
         <button type="submit">Save</button>
       </form>
+      <h2>Contacts on this deal</h2>
+      <ul style={{ listStyle: "none", padding: 0 }}>
+        {contacts.map((c) => (
+          <li key={c.id} style={{ marginBottom: "0.5rem" }}>
+            {c.name} ({c.email}) — <button type="button" onClick={() => removeContact(c.id)} style={{ fontSize: "0.875rem" }}>Remove</button>
+          </li>
+        ))}
+      </ul>
+      {availableToAdd.length > 0 && (
+        <>
+          <h3>Add existing contact</h3>
+          <select onChange={(e) => { const v = e.target.value; if (v) addContact(v); e.target.value = ""; }}>
+            <option value="">— Select contact —</option>
+            {availableToAdd.map((c) => (
+              <option key={c.id} value={c.id}>{c.name} ({c.email})</option>
+            ))}
+          </select>
+        </>
+      )}
     </main>
   );
 }
diff --git b/packages/nestjs-vite-react/src/pages/NewDeal.tsx a/packages/nestjs-vite-react/src/pages/NewDeal.tsx
index 90b65fca..385850c2 100644
--- b/packages/nestjs-vite-react/src/pages/NewDeal.tsx
+++ a/packages/nestjs-vite-react/src/pages/NewDeal.tsx
@@ -6,15 +6,26 @@ export default function NewDeal() {
   const [name, setName] = useState("");
   const [description, setDescription] = useState("");
   const [address, setAddress] = useState("");
+  const [contactName, setContactName] = useState("");
+  const [contactEmail, setContactEmail] = useState("");
+  const [contactPhone, setContactPhone] = useState("");
   const [loading, setLoading] = useState(false);
 
   const submit = (e: React.FormEvent) => {
     e.preventDefault();
     setLoading(true);
+    const body: { name: string; description: string; address: string; contact?: { name: string; email: string; phone: string } } = {
+      name,
+      description,
+      address,
+    };
+    if (contactName && contactEmail && contactPhone) {
+      body.contact = { name: contactName, email: contactEmail, phone: contactPhone };
+    }
     fetch("/api/deals", {
       method: "POST",
       headers: { "Content-Type": "application/json" },
-      body: JSON.stringify({ name, description, address }),
+      body: JSON.stringify(body),
     })
       .then(() => navigate("/thank-you"))
       .finally(() => setLoading(false));
@@ -29,6 +40,10 @@ export default function NewDeal() {
         <div><label>Deal / Customer name</label><input value={name} onChange={(e) => setName(e.target.value)} required /></div>
         <div><label>Description</label><textarea value={description} onChange={(e) => setDescription(e.target.value)} required /></div>
         <div><label>Address</label><input value={address} onChange={(e) => setAddress(e.target.value)} required /></div>
+        <h2>Primary Contact (optional)</h2>
+        <div><label>Contact name</label><input value={contactName} onChange={(e) => setContactName(e.target.value)} /></div>
+        <div><label>Contact email</label><input type="email" value={contactEmail} onChange={(e) => setContactEmail(e.target.value)} /></div>
+        <div><label>Contact phone</label><input type="tel" value={contactPhone} onChange={(e) => setContactPhone(e.target.value)} /></div>
         <button type="submit" disabled={loading}>{loading ? "Submitting..." : "Submit Deal"}</button>
       </form>
     </main>
